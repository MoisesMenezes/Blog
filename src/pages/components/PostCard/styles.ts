import styled from "styled-components";

export const Container = styled.button`
  cursor: pointer;
  background: transparent;
  border: none;
  max-width: 800px;

  h2 {
    text-align: left;
  }
`;
